rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access to all documents
    // This is for development purposes only and is not secure.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
